@model IEnumerable<WebApp.Models.ActualInDay>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActualInMonthD.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DayInMonth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfShipments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActualInMonthD.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DayInMonth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfShipments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
